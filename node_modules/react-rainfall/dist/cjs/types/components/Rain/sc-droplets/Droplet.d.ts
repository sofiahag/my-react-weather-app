import type { dropletColor } from '../rainTypes';
interface DropletProps {
    dropletColor: dropletColor;
}
declare const Droplet: import("styled-components").StyledComponent<"div", any, DropletProps, never>;
export default Droplet;
