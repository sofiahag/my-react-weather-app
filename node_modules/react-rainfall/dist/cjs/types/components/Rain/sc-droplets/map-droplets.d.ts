import type { dropletOptions } from '../rainTypes';
declare const mapDroplets: (rainRef: {
    maxWidth: number;
    maxHeight: number;
}, dropletOptions: dropletOptions) => Array<any>;
export default mapDroplets;
