import type { size } from '../rainTypes';
interface DropContainerProps {
    gapLength: number;
    maxHeight: number;
    size: size;
}
declare const DropletContainer: import("styled-components").StyledComponent<"div", any, DropContainerProps, never>;
export default DropletContainer;
